# superseded prototype, kept locally for reference only (see HANDOFF.md)
_legacy
_bak
_blockies
_samples

# OS / editor
.DS_Store
*.swp

# node (library has no runtime deps, but tooling may add a node_modules locally)
node_modules/

# bu
_buignore


# vim backup
*~
